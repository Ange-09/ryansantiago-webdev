import ContactSection from "./ContactSection";

export const metadata = {
  title: "Contact — VERVE",
};

export default function ContactPage() {
  return (
    <main>
      <ContactSection />
    </main>
  );
}
